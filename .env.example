# Copy this file to .env and set a strong random key for production.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
AUDIT_SECRET_KEY=replace-with-strong-random-key
MODEL_PROVIDER=mock
